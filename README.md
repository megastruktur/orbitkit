# OrbitKit

Reusable, domain-neutral shell SDK: replaceable mascot, configurable arc/radial menu,
main app + detached popups. Platforms: Windows / macOS / Android (system overlay above
other apps).

Status: campaign `android-feasibility` in progress — see `plans/` (local, untracked).
