[package]
name = "starter"
version = "0.1.0"
description = "OrbitKit starter consumer app (Tauri v2)"
edition = "2021"

[lib]
name = "orbitkit_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[features]
default = []
recorder = ["dep:tauri-plugin-orbitkit-recorder"]
wry = ["tauri/wry"]

[build-dependencies]
tauri-build = { version = "=2.6.3", features = [] }

[dependencies]
tauri = { version = "=2.11.6", features = ["macos-private-api"] }
tauri-plugin-orbitkit = { path = "../../../crates/tauri-plugin-orbitkit" }
tauri-plugin-orbitkit-recorder = { path = "../../../crates/tauri-plugin-orbitkit-recorder", optional = true }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
log = "0.4"
jni = { version = "0.21", default-features = false }
